Display a CLI name, category, command, and observed repository metrics. Use `href` for navigation or `onClick` for a preview detail view. Missing metrics display a dash.

```jsx
<ToolCard name="Example CLI" command="example --help" stars="—" category="Developer tools" />
```
