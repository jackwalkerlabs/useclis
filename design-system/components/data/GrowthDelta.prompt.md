One-line: MoM / 30-day growth readout for leaderboard rows and metric blocks.

```jsx
<GrowthDelta value={11} period="MoM" />
```

Zero and missing values render grey with an en-dash rather than green 0%.
