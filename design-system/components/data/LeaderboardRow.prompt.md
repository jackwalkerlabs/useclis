Rank a CLI by observed repository data. Keep counts in monospace; omit growth when there are not enough dated observations.

```jsx
<LeaderboardRow rank={1} name="Example CLI" command="example" stars="—" href="/tools/example/" />
```
