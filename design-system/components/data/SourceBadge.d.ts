import * as React from "react";
/** Names the source of repository data; does not certify the tool. */
export interface SourceBadgeProps { source?: string; compact?: boolean; children?: React.ReactNode; }
export declare function SourceBadge(props: SourceBadgeProps): React.JSX.Element;
