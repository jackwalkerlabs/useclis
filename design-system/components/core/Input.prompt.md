One-line: the standard text field — search boxes, asking-price fields, API-key paste.

```jsx
<Input label="Search CLIs" iconLeft={<SearchIcon/>}  placeholder="Command or repository" />
```

Focus draws a near-black border plus green focus ring; `error` turns the border red and replaces the hint.
