One-line: the container for everything — 1px subtle border, 12px radius, near-flat shadow.

```jsx
<Card as="a" href="/tools/github-cli/" interactive>…</Card>
```

`accentBg` exists because paid listing tiers can set a custom card background; leave it unset for free listings.
