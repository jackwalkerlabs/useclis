One-line: square-rounded for repository avatars, `shape="circle"` for maintainer photos.

```jsx
<Avatar src={logo} name="Example CLI" size={40} />
<Avatar name="Example maintainer" shape="circle" size={24} />
```
