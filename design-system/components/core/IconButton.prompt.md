One-line: icon-only button for dense chrome (share, bookmark, theme toggle) — always pass `label`.

```jsx
<IconButton label="Share listing" variant="outline"><ShareIcon/></IconButton>
```

Sizes 30/36/42px. Ghost is the default and only darkens its glyph on hover.
