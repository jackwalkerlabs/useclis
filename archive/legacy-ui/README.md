# Earlier directory styles

The complete stylesheet before unused-layout cleanup, preserved at the owner's request for future reference. It includes the earlier sidebar, card-grid, list-view, and detail-panel styles, along with the then-current page styles. Token import paths are adjusted for this archive location.

This file is not imported by the app or included in production output. When reusing a layout, move the relevant rules into active styles and check them against the current markup and responsive behavior. Maintained reusable components and tokens live in `design-system/`.
